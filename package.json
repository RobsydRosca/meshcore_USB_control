{
    "name": "meshcore_USB_control",
    "description": "USB interface to handle a meshcore companion USB",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}